export type TTodoCardProps = {
  _id: string;
  title: string;
  description: string;
  isCompleted?: boolean;
  priority: string;
};

export type TId = {
  id: string;
};
export type TUnderId = {
  _id: string;
};
